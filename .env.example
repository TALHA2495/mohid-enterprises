# Copy to .env.local and fill in real values. Never commit .env.local.

# WhatsApp Business Click-to-Chat number (international format, no + or spaces)
NEXT_PUBLIC_WHATSAPP_NUMBER=92XXXXXXXXXX

# Admin dashboard password (proxy.ts + /api/admin/login)
ADMIN_PASSWORD=change-me

# Supabase (Settings → API in the Supabase dashboard)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...            # public, client-safe (RLS-locked)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...                # SECRET — server-side only, bypasses RLS
